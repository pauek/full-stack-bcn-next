
In Javascript, "objects" are all non-primitive types: **Functions**, **Objects** (i.e. "structs"), and **Arrays**. (And more specific classes like `Date`s, `Map`s and `Set`s).

This session explores both the basics and types of Functions, Objects and Arrays. 